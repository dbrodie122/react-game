import React from 'react';

const Button = (props) => {
  return (
    <button onClick={ props.onClick }>
      button
      <style jsx>{`

      `}</style>
    </button>
  )
}

export default Button;